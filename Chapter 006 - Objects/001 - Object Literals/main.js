"use strict";

const empty = {}; //An object with no properties
console.log(empty);
console.log();
const point = {
    //An object with two numeric properties
    x: 1,
    y: 2,
};
console.log(point);
console.log();

const book = {
    "main title": "JavaScript",
    "sub-title": "The Definive Guide",
    author: {
        firstName: "David",
        lastName: "Flanagan",
    },
};
console.log(book);
console.log();
