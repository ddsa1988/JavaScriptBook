"use strict";

const myObj = new Object(); //Create an empty object: same as {}.
const myArray = new Array(); //Create an empty array: same as [].
const date = new Date(); // Create a Date object representing the current time.

console.log(myObj);
console.log(myArray);
console.log(date);
