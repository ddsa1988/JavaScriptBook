"use strict";

const random = Math.floor(Math.random() * 10 + 1);
// console.log(random);

if (random < 6) {
    console.log(`The random number ${random} is less than  5.`);
} else {
    console.log(`The random number ${random} is greater than 5.`);
}
